{
  "name": "@xlit/router",
  "version": "2.4.0",
  "description": "xlit router custom element",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "sideEffects": false,
  "files": [
    "/index.{js,js.map}",
    "/*.{d.ts,d.ts.map}",
    "/middlewares/"
  ],
  "scripts": {
    "prepublish": "pnpm clean; pnpm lint && pnpm build",
    "clean": "rm -rf coverage middlewares index.js *.d.ts *.map",
    "lint": "wireit",
    "lint:eslint": "wireit",
    "lint:ts": "wireit",
    "build": "wireit",
    "build:js": "wireit",
    "build:types": "wireit",
    "test": "wireit",
    "test:watch": "wtr --watch"
  },
  "wireit": {
    "lint": {
      "dependencies": [
        "lint:eslint",
        "lint:ts"
      ]
    },
    "lint:eslint": {
      "command": "eslint ./src",
      "files": [
        "./src/**/*.ts"
      ],
      "output": []
    },
    "lint:ts": {
      "command": "tsc --noEmit",
      "files": [
        "./src/**/*.ts"
      ],
      "output": []
    },
    "build": {
      "dependencies": [
        "build:js",
        "build:types"
      ]
    },
    "build:js": {
      "command": "esbuild ./src/index.ts ./src/middlewares/*.ts --packages=external --external:../index.js --bundle --format=esm --minify --sourcemap --outdir=./",
      "files": [
        "./src/**/*.ts"
      ],
      "output": [
        "./index.js{,.map}",
        "./middlewares/*.js{,.map"
      ]
    },
    "build:types": {
      "command": "tsc -p tsconfig.build.json --emitDeclarationOnly",
      "files": [
        "./src/**/*.ts"
      ],
      "output": [
        "*.d.ts{,.map}"
      ]
    },
    "test": {
      "command": "wtr",
      "files": [
        "./src/**/*.ts"
      ],
      "output": []
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reekoheek/xlit.git"
  },
  "keywords": [
    "router",
    "customelements",
    "webcomponents"
  ],
  "author": "Ganesha <reekoheek@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/reekoheek/xlit/issues"
  },
  "homepage": "https://github.com/reekoheek/xlit#readme",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "testutil": "workspace:*"
  }
}
