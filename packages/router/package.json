{
  "name": "@xlit/router",
  "version": "2.2.1",
  "description": "xlit router custom element",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "sideEffects": false,
  "files": [
    "/index.{js,js.map}",
    "/*.{d.ts,d.ts.map}"
  ],
  "scripts": {
    "prepublish": "pnpm clean; pnpm build",
    "clean": "rm -rf coverage index.js *.d.ts *.map",
    "lint": "pnpm lint:eslint && pnpm lint:ts",
    "lint:eslint": "eslint ./src",
    "lint:ts": "tsc --noEmit",
    "build": "pnpm lint && pnpm build:js && pnpm build:types",
    "build:js": "esbuild ./src/index.ts --packages=external --bundle --format=esm --minify --sourcemap --outdir=./",
    "build:types": "tsc --emitDeclarationOnly",
    "test": "wtr",
    "test:watch": "wtr --watch",
    "examples": "vite ./examples"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reekoheek/xlit.git"
  },
  "keywords": [
    "router",
    "customelements",
    "webcomponents"
  ],
  "author": "Ganesha <reekoheek@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/reekoheek/xlit/issues"
  },
  "homepage": "https://github.com/reekoheek/xlit#readme",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "testutil": "workspace:*"
  }
}
