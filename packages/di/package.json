{
  "name": "@xlit/di",
  "version": "2.0.2",
  "description": "xlit dependency injection custom element",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "files": [
    "/dist/"
  ],
  "scripts": {
    "prepublish": "pnpm clean; pnpm build",
    "clean": "rm -rf dist coverage",
    "lint": "pnpm lint:eslint && pnpm lint:ts",
    "lint:eslint": "eslint ./src",
    "lint:ts": "tsc --noEmit",
    "build": "pnpm lint && pnpm build:js && pnpm build:types",
    "build:js": "esbuild ./src/index.ts --bundle --platform=neutral --minify --sourcemap --outdir=./dist",
    "build:types": "tsc --emitDeclarationOnly",
    "test": "wtr",
    "examples": "vite ./examples"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reekoheek/xlit.git"
  },
  "keywords": [
    "di",
    "dependency injection",
    "customelements",
    "webcomponents"
  ],
  "author": "Ganesha <reekoheek@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/reekoheek/xlit/issues"
  },
  "homepage": "https://github.com/reekoheek/xlit#readme",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "esbuild": "^0.17.18",
    "wireit": "^0.9.5",
    "testutil": "workspace:*"
  }
}
