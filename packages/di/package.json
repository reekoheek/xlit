{
  "name": "@xlit/di",
  "version": "2.7.5",
  "description": "xlit dependency injection container",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "files": [
    "/dist"
  ],
  "scripts": {
    "prepublishOnly": "pnpm checkall",
    "clean": "rm -rf .wireit coverage dist",
    "checkall": "pnpm lint && pnpm test && pnpm build",
    "lint": "wireit",
    "build": "wireit",
    "test": "wireit",
    "test:watch": "vitest watch --coverage"
  },
  "wireit": {
    "lint": {
      "command": "eslint ./src && tsc --noEmit",
      "files": [
        "./src/**/*.ts"
      ],
      "output": []
    },
    "build": {
      "command": "esbuild ./src/index.ts --packages=external --bundle --platform=neutral --minify --sourcemap --outdir=./dist && tsc -p tsconfig.build.json --emitDeclarationOnly",
      "files": [
        "./src/**/*.ts"
      ],
      "output": [
        "./dist/**"
      ]
    },
    "test": {
      "command": "vitest run --coverage",
      "files": [
        "./src/**/*.ts"
      ],
      "output": [
        "./coverage/**"
      ]
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reekoheek/xlit.git"
  },
  "keywords": [
    "di",
    "dependency injection",
    "customelements",
    "webcomponents"
  ],
  "author": "Ganesha <reekoheek@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/reekoheek/xlit/issues"
  },
  "homepage": "https://github.com/reekoheek/xlit#readme",
  "publishConfig": {
    "access": "public"
  }
}
